# Banco (padrão SQLite p/ rodar na hora). Para produção use Postgres/Neon.
DATABASE_URL="file:./dev.db"
# Segredo para assinar a sessão (troque em produção)
AUTH_SECRET="troque-este-segredo-em-producao"
